# Optional: used by Supabase config (e.g. Edge Functions / AI).
# Only needed if you enable features that use OpenAI in config.toml.
# For local ticketing (tickets table), the NestJS API uses its own env (see api/.env.example).

# OPENAI_API_KEY=sk-...
