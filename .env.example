# Ava monorepo — env vars overview
# Copy to .env at the root if you use a single env file, or set vars in each app directory.
#
# API (api/)
#   PORT=4000
#   SUPABASE_URL=http://127.0.0.1:54321
#   SUPABASE_SERVICE_ROLE_KEY=<from: supabase status>
#   SUPABASE_JWT_SECRET=<from: supabase status, "JWT secret">  # required for admin auth
#
# Admin (admin/)
#   NEXT_PUBLIC_API_URL=http://localhost:4000
#   NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
#   NEXT_PUBLIC_SUPABASE_ANON_KEY=<from: supabase status>
#
# Guest app (guest-app/)
#   EXPO_PUBLIC_API_URL=http://localhost:4000
#
# Supabase (optional, for Edge Functions / config)
#   OPENAI_API_KEY=sk-...
