# Expo guest app — copy to .env in this directory
# API base URL (no trailing slash). Use your machine IP for physical device (e.g. http://192.168.1.x:4000).

EXPO_PUBLIC_API_URL=http://localhost:4000
