# NestJS API — copy to .env in this directory
# Get Supabase URL and Secret key after running: supabase status (from project root)

PORT=4000
SUPABASE_URL=http://127.0.0.1:54321
# Service role / Secret key — for DB access and for validating admin JWTs via Auth API
SUPABASE_SERVICE_ROLE_KEY=your_secret_key_from_supabase_status

# OpenAI — required for chat stream (guest app)
OPENAI_API_KEY=sk-...
OPENAI_CHAT_MODEL=gpt-3.5-turbo
